DATABASE_URL="file:./dev.db"

# Firma de las sesiones. En un servidor de verdad esto va fuera del repositorio.
JWT_SECRETO="cambia-esto-en-produccion-por-algo-largo-y-aleatorio"
JWT_DURACION="7d"

# Puerto de la API y quién puede llamarla desde el navegador.
PORT=3001
ORIGENES="http://localhost:3000"
